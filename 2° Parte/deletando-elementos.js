// Supondo que um professor acidentalmente lançou 5 notas no sistema para cada aluno
// E para corrigirmos isso temos que remover a última nota lançada, para isso usamos o método pop()

const notas = [10, 5, 4, 5, 7];

console.log(notas);
notas.pop();
console.log(notas);

// split
// O método split é usado para dividir uma string em um array de 
// substrings com base em um separador especificado. Se o separador 
// não for fornecido, a string inteira será colocada como o único 
// elemento do array.

// join
// O método join é usado para concatenar todos os elementos de um array 
// em uma única string. Um separador opcional pode ser especificado para 
// separar os elementos na string resultante. Se nenhum separador for fornecido,
//  os elementos são separados por uma vírgula por padrão.
